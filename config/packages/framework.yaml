framework:
    secret: '%env(APP_SECRET)%'
    router:
        utf8: true
    test: true
    http_method_override: false
    serializer: ~
    validation: { enable_attributes: true }
    session: false
